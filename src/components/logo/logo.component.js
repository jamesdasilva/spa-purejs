import './logo.scss';

const template = () => `
  <div class="logo">
    <div class="logo__hexagon"></div>
    <div class="logo__text">UX</div>
  </div>`;

const Logo = {
  template,
  events: { },
};

export default Logo;
